# Secrets — kabhi commit mat karo
data/config.json
data/auth.json
*.env

# Logs aur results — local only
*.log
results/
__pycache__/
*.pyc
*.pyo
venv/

# Trade DB (order_store) — local/VPS runtime data, never commit
data/trades.db
data/trades.db-wal
data/trades.db-shm

# Config files (sensitive — VPS pe manually rakho)
nifty_config.json
rsi_config.json

# Health-check + alert runtime output (per-run, har baar badalta)
data/health_report.json
data/downloader_alert.json
# Notification centre ka runtime state (b51cb21/5fc889b ke saath aaya, ignore
# karna reh gaya tha — dashboard chalte hi ye untracked ban ke `git status` me
# baithe rehte hain aur ek `git add -A` inhe commit kar deta)
data/notifications.jsonl
data/alert_ingest_seen.json
data/error_watch_offsets.json
# Broker balance snapshots + uploaded ledger CSVs/XLSX — REAL financial data.
# algo-trader repo is PUBLIC → these must NEVER be committed (2026-08-03).
data/broker_balance_history.json
data/broker_ledger_*.json

# Runtime data — local/VPS only, code nahi (large, har run badalta)
_TRADING_DATA/
data/note_images/
# Idea Vault — user-uploaded idea/strategy clips + their index (large video files,
# per-user; algo-trader repo is PUBLIC so these must never be committed).
data/idea_clips/
data/idea_vault.json
data/shared_ltp_cache.json
data/backtest.db
data/dhan_rate_limiter.db
data/kite_rate_limiter.db
data/downloader_log.json
data/sec_id_cache.json
data/opt_pnl_cache.json
data/trade_log.json
data/rsi_v1_watch.json

# ─────────────────────────────────────────────────────────────────────────
# 2026-07-09 drift cleanup — runtime state + scratch that kept re-appearing
# ─────────────────────────────────────────────────────────────────────────
# Regenerated data (daily / per-run) — never belongs in git
data/api-scrip-master.csv
data/dhan_rate_limit_events.json
data/master_*.flag
data/daily_state.json
data/dhan_feed_owner.db
data/kite_trades_*.json
data/peak_pnl_history*.json
data/shared_candle_cache.json
data/tags_store.json
data/bt_presets.json
data/fill_confirm_delays.json
data/ltp_watch_extra.json
data/nifty_daily.csv
data/watch_*.json
data/trailing_lock_fired_*.txt
data/pos_lock_state.json
data/tsl_state.json
data/kill_floor_state.json
data/rms_audit_log.json
data/*.bak
data/*.bak.*
data/*.bak_*
orders.db
*.bak.split.*
nifty_config.json.bak*

# Bulletproof: NEVER commit any sqlite DB (live trade/runtime data — must never
# reach the public GitHub repo, esp. now that VPS is also a git repo)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# node / puppeteer experiments
node_modules/
package.json
package-lock.json

# one-off scratch / debug scripts (never deployed)
scratch/
IMG FOR CLAUD/
check*.py
clean.py
delete_*.py
fix_*.py
patch_*.py
recover_*.py
find_block.py
extract_base64.py
scratch_tags.py
verify.py
test_*.js
test_extract.py
dump*.js
dump*.html
puppeteer_*.js
real_missing.js
live_cal_render.js
cal_render.js
badge.js
missing.txt
cols.txt
old_index.html
old_render.txt
codebase_graph.html
ui_mockup_redesign.html
sl_visualizer*.html
vps_screenshot.png
vps_delete_script.py

# EOD reports — generated output (eod_report.py), commit nahi hote
data/reports/
scratch/nifty_trend/ml_mining_log.csv

# Lab gzip cache — sidecar .gz for /lab static assets (serve_lab_file), auto-regen per-machine
scratch/nifty_trend/**/*.gz
data/audit_history/

# SL/exit analysis script outputs — per-run generated artifacts, not source
data/path_aware_sl_sim.csv
data/exit_reason_analysis.csv

# ad-hoc API response dumps (debug artifacts)
resp.json

# Claude Code session write-lock (see .claude/session_guard.py) — per-machine, never commit
.claude/.session_lock.json

# Live VPS config snapshots — has webhooks.global.secret_token, NEVER commit
# (algo-trader is a PUBLIC repo). Off-VPS backup only.
_config_backups/

# gzip sidecars generated at runtime by the static-gzip routes (trader_dashboard.py)
static/js/*.gz
static/vendor/*.gz
scratch/nifty_trend/runs/*/*.gz

data/telegram_config.json
